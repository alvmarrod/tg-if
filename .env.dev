RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

LOG_LEVEL=INFO

# How should subscribers reach lazy-loaded files?
API_SIDE_PORT=8080
MEDIA_BASE_URL=http://tg-if

# Admin notifier bot config is in config/bots.json under the "admin" key.
# Requires a dedicated Telegram bot session and an admin user_id.